# Install required packages
steps:
  - script: |
      sudo apt-get clean
      sudo apt-get update
      sudo apt-get install xvfb
    displayName: "* install required packages"
    condition: succeeded()
